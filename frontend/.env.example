# API のベース URL。
# dev は Vite の dev proxy で /api を同一オリジン化するため空(相対 /api)にする (ADR-0004 / ADR-0009)。
# 別オリジンの API を直接叩く場合のみ絶対 URL を設定する。
VITE_API_BASE_URL=

# Vite dev proxy の転送先(Go バックエンド)。dev のみ使用。
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
