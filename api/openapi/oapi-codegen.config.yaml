# openapi.yaml から Go の型(モデル)を生成する設定 (api ADR-0005)。
# 生成は types-only。出力先・spec パスはこのディレクトリ(api/openapi)からの相対。
package: apigen
output: ../internal/apigen/apigen.gen.go
generate:
  models: true
output-options:
  skip-prune: true
