# openapi.yaml から Go の型(モデル)を生成する設定 (api ADR-0005)。
# 生成は types-only。既存ハンドラ/DTO はこの段階では差し替えない。
package: apigen
output: internal/apigen/apigen.gen.go
generate:
  models: true
output-options:
  skip-prune: true
